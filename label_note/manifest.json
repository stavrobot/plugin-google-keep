{
  "name": "label_note",
  "description": "Add or remove a label from a Google Keep note.",
  "entrypoint": "run.py",
  "parameters": {
    "note_id": {
      "type": "string",
      "description": "The ID of the note to label."
    },
    "label": {
      "type": "string",
      "description": "The name of the label to add or remove."
    },
    "remove": {
      "type": "boolean",
      "description": "If true, remove the label instead of adding it."
    }
  }
}
